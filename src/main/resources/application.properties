## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:oracle:thin:@//192.168.10.109:1521/si00
spring.datasource.username =starfrx
spring.datasource.password =Frx1720 

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# = JPA / HIBERNATE
# Show or not log for each sql query
spring.jpa.show-sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.Oracle10gDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
server.port = 4200

spring.datasource.dbcp2.initial-size=100
spring.datasource.dbcp2.max-total=1000
spring.datasource.dbcp2.pool-prepared-statements= true

#oracle
debug=false